/**
 * Backlog API Configs
 */

/** Debug */
export const API_ENDPOINT = "https://flighter.backlog.com";

export const urls = {
    oauth: `${API_ENDPOINT}/OAuth2AccessRequest.action`,
    request_access_token: `${API_ENDPOINT}/api/v2/oauth2/token`
}
